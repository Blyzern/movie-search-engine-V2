import styled from 'styled-components';

export const Wrapper = styled.div`
    position: absolute;
    background-color: #141414;
    width: fit-content;
    height: fit-content;
    display: flex;

    ::-webkit-scrollbar {
        width: 0px;
    }
    
`;