export const isSerieSelector = (state) => state.description.isSerie;

export const dataSelector = (state) => state.description.data;

export const movieIdSelector = (state) => state.description.movieId;

export const isLoadingSelector = (state) => state.description.isLoading;
